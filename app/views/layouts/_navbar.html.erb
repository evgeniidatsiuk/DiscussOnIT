<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarTogglerDemo03" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item active">
        <%=link_to "Головна сторінка" , root_path, class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%=link_to "Питання" , questions_path, class:"nav-link"%>
      </li>
      <li class="nav-item">
        <%=link_to "Записи" , posts_path, class:"nav-link"%>
      </li>
      <li class="nac-item">
        <%=link_to "Категорії", categories_path, class:"nav-link"%>
      </li>
    </ul>

    <form class="form-inline my-2 my-lg-0" style="padding:0px 15px 0px;">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>

    <ul class="navbar-nav">
      <%if user_signed_in?%>
        <span class="navbar-text">
          <%= current_user.userparam.nickname %>
        </span>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbardrop" data-toggle="dropdown">
            <img class="img-circle" style="margin-top: -4px; margin-bottom: -4px;" height="30px" src="<%=current_user.userparam.avatar.url%>">
          </a>
          <div class="dropdown-menu dropdown-menu-right">
            <p class="dropdown-item">
              <span class="fa fa-user" style="color: lightgray;"></span>&nbsp;<%= link_to t('profile'), userparam_path(current_user.userparam.id)%></p>
            <p class="dropdown-item">
              <span class="fa fa-envelope " style="color: lightgray;"></span>&nbsp;<%= link_to "Сповіщення", notifications_path%>
              <% if Notification.where(user_id: current_user.id).count > 0%>
                <span class="badge badge-primary" data-toggle="tooltip" title="Кількість непрочитаних">
                  <%= Notification.where(user_id: current_user.id).count%>
                </span>
              <%end%>
            </p>
            <hr>
            <p class="dropdown-item">
              <span class="fa fa-sign-out" style="color: lightgray;"></span>&nbsp;<%= link_to t('sign_out'), destroy_user_session_path,method: :delete%></p>

          </div>
        </li>
        <li class="nav-item"></li>
      <%else%>
        <li class="nav-item">
          <%= link_to t('sign_in'), new_user_session_path, class:'nav-link'%>
        </li>
      <%end%>
    </ul>
  </div>
</nav>
